version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Officials_Identity
      - Officials_DB=Officials_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
